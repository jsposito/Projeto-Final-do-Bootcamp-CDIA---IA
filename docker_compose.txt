version: '3.8'

services:
  jupyter-ml:
    build: .
    container_name: bootcamp_ml_project
    ports:
      - "8888:8888"
    volumes:
      - ./data:/app/data
      - ./notebooks:/app/notebooks
      - ./models:/app/models
      - ./outputs:/app/outputs
    environment:
      - JUPYTER_ENABLE_LAB=yes
    networks:
      - ml-network

networks:
  ml-network:
    driver: bridge

volumes:
  ml-data: